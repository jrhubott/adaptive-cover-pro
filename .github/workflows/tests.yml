name: Tests

on:
  workflow_dispatch:
  pull_request:
  push:
    branches:
      - main

jobs:
  test:
    name: Test (Python ${{ matrix.python-version }} / HA ${{ matrix.ha-channel }})
    runs-on: ubuntu-latest
    continue-on-error: ${{ matrix.experimental }}
    strategy:
      fail-fast: false
      matrix:
        include:
          - python-version: "3.13"
            ha-channel: stable
            experimental: false
          - python-version: "3.13"
            ha-channel: beta
            experimental: true

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Install dependencies (stable)
        if: matrix.ha-channel == 'stable'
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements-dev.txt

      - name: Install dependencies (beta — pre-release HA)
        if: matrix.ha-channel == 'beta'
        run: |
          python -m pip install --upgrade pip
          pip install --pre pytest-homeassistant-custom-component
          pip install --pre -r requirements-dev.txt

      - name: Show HA version
        run: python -c "from homeassistant.const import HAVERSION; print('HA version:', HAVERSION)"

      - name: Run tests with coverage
        run: |
          pytest --cov --cov-report=xml --cov-report=term

      - name: Upload coverage to Codecov (stable only)
        if: matrix.python-version == '3.13' && matrix.ha-channel == 'stable'
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}
