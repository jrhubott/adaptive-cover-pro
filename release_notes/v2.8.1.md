## 🎯 v2.8.1 — Engine Refactor & Notebook Updates

### 🏗️ Refactoring

- **Cover geometry modules** — `AdaptiveGeneralCover`, `AdaptiveVerticalCover`, `AdaptiveHorizontalCover`, and `AdaptiveTiltCover` moved from `calculation.py` into individual modules under `engine/covers/` (`base.py`, `vertical.py`, `horizontal.py`, `tilt.py`), matching the pattern established by `venetian.py`. `calculation.py` now contains only state/orchestration classes with backward-compatible re-exports
- **`venetian.py`** updated to import siblings directly instead of reaching back into `calculation.py`
- **`coordinator.py`** updated to import geometry classes from canonical `engine.covers` path

### 🐛 Bug Fixes

- Fixed ruff lint configuration — `.ruff.toml` now correctly excludes `notebooks/` directory
- Fixed D401 docstring mood warning in `coordinator._async_update_data`

### 📚 Documentation

- Restored `notebooks/test_env.ipynb` (previously deleted) updated to use current typed config API (`CoverConfig`, `VerticalConfig`, `HorizontalConfig`)
- Re-executed `notebooks/simulate_cover.ipynb` to verify it runs cleanly against the current codebase

### 🧪 Testing

- 752 tests passing, 0 failures

### 🔧 Installation

Download `adaptive_cover_pro.zip` from the assets below and extract to your `custom_components/` directory, or update via HACS.

Requires Home Assistant 2024.5.0+.
