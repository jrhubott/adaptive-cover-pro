## 🐛 Fix: Sync categories match split screens

### Bug Fix

- **Sync categories mismatch** — The "Copy Settings to Other Covers" selector was showing legacy "Climate" and "Weather Conditions" categories that no longer matched the options menu. Now correctly shows "Light Sensors & Cloud Suppression" and "Temperature & Climate Mode" to match the split screens introduced in beta.1.

### Details

- Legacy `climate` and `weather` category keys remain internally for backward compatibility with programmatic callers
- New `_SYNC_UI_CATEGORIES` list controls which categories appear in the sync selector UI
- Confirmation screen labels updated to match new category names
- 6 new tests verifying UI/legacy separation and full key coverage

### 🧪 Testing

- 1174 tests passing (6 new)
- Tested with Python 3.11 and 3.12
- Home Assistant 2024.5.0+
