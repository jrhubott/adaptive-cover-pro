# v2.7.5-beta.3 - Force Override Sensors (Beta)

## 🎯 Weather Safety Protection

This beta release introduces force override sensors - a powerful safety feature that allows binary sensors to override automatic cover control for a specific integration instance.

## ✨ New Features

### Force Override Sensors
- **Weather Safety Protection**: Configure binary sensors (rain, wind, etc.) that override automatic control when active
- **Per-Instance Control**: When ANY configured sensor is active ("on"), ALL covers in that integration instance move to a configured safe position
- **Customizable Safe Position**:
  - Default: 0% (fully retracted/closed) - ideal for rain/wind protection
  - Configurable: Set to 100% for security sensors requiring emergency access
- **Manual Override Preserved**: Manual control still works during force override
- **Configuration Variables**:
  - `force_override_sensors`: List of binary sensor entity IDs to monitor
  - `force_override_position`: Target position (0-100%) when override is active

### Use Cases
- **Rain Protection**: Link to rain sensor → automatically retract awnings
- **Wind Protection**: Link to wind sensor → close blinds during high winds
- **Security**: Link to alarm sensor → open all covers for emergency access
- **Multiple Conditions**: Monitor multiple sensors simultaneously (ANY active triggers override)

## 🧪 Testing

This is a **beta release** for testing purposes. Please test thoroughly before using in production:

### Testing Checklist
- ✅ Configure force override sensors in integration options
- ✅ Test with binary sensor turning on/off
- ✅ Verify all covers move to configured position when sensor activates
- ✅ Verify automatic control resumes when sensor deactivates
- ✅ Test manual control works during force override
- ✅ Test with multiple sensors (any active should trigger override)

### Known Limitations
- Force override is configured per integration instance (each instance can have different sensors)
- Within an instance, force override applies to ALL covers (no per-cover configuration)

## 📦 Installation

**Beta Testing via HACS:**
1. Ensure you have HACS installed
2. Add custom repository: `https://github.com/jrhubott/adaptive-cover`
3. Install version `v2.7.5-beta.3`
4. Restart Home Assistant
5. Update integration options to configure force override sensors

**Manual Installation:**
1. Download `adaptive_cover_pro.zip` from this release
2. Extract to `custom_components/adaptive_cover_pro/`
3. Restart Home Assistant

## 🔧 Technical Details

- Added `force_override_sensors` configuration option (list of entity IDs, per-instance)
- Added `force_override_position` configuration option (default: 0%, per-instance)
- Each integration instance monitors its own configured sensors for state changes
- Force override takes precedence over all other control logic within the instance
- Multiple integration instances can have different force override sensors and positions
- New test coverage: 258 tests added for force override behavior

## ⚠️ Beta Warning

This is a **pre-release version** for testing. Please report any issues at:
https://github.com/jrhubott/adaptive-cover/issues

## 📋 Requirements

- Home Assistant 2024.5.0+
- Python 3.11+

## 🐛 Reporting Issues

If you encounter any problems:
1. Check existing issues: https://github.com/jrhubott/adaptive-cover/issues
2. Create new issue with:
   - Force override sensor configuration
   - Binary sensor states during the issue
   - Debug logs from integration
   - Expected vs actual behavior
