## 🎨 Icon Branding & HACS Visibility

### ✨ Features

- **Local brand icons** — Added `custom_components/adaptive_cover_pro/brand/` folder with `icon.png`, `dark_icon.png`, `logo.png`, and `dark_logo.png`. Home Assistant 2026.3+ loads icons from this local path (takes priority over the CDN), ensuring the integration's icon appears correctly in the integration cards and device pages.
- **HACS default submission** — Submitted `jrhubott/adaptive-cover-pro` to the HACS default repository for native discoverability in the HACS UI.
- **home-assistant/brands submission** — Submitted brand icons to the `home-assistant/brands` CDN for older HA versions.

### 🧪 Testing

- All 313 tests passing
- Local brand folder verified against HA 2026.3+ icon loading spec

### 📦 Installation

Install or update via HACS, or copy `custom_components/adaptive_cover_pro/` to your `config/custom_components/` directory.
