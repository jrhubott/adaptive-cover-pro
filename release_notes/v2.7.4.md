## 🐛 Critical Import & Compatibility Fix

### Bug Fixes

**Fixed blocking I/O warnings and import errors**

- Resolved `NameError: name 'callback' is not defined` during entity setup
- Fixed blocking I/O warnings with Home Assistant 2026.2.1 (Python 3.13)
- Moved improper import statements to module level following Python best practices (PEP 8)

### Technical Details

**Root Cause:**
Two import statements were placed incorrectly:

1. **entity_base.py (Line 106)**: Import inside class definition

   - Broke module namespace during import
   - Caused `NameError` for `callback` decorator
   - Home Assistant module loader detected as blocking I/O

2. **button.py (Line 23)**: Import inside async function
   - Triggered blocking I/O warnings in event loop
   - Home Assistant 2026.2+ has stricter async checks (Python 3.13)

**Solution:**

- Moved `EntityCategory` import to module level in entity_base.py
- Moved `DOMAIN` import to module level in button.py
- Follows Python PEP 8 conventions for proper import organization

### Impact

**Before Fix:**

```
NameError: name 'callback' is not defined
ImportError: Exception importing custom_components.adaptive_cover_pro.sensor
Detected blocking call to import_module inside the event loop
```

**After Fix:**

```
✅ Integration loads without errors
✅ No blocking I/O warnings
✅ All entities created successfully
✅ Migration from AdaptiveCover works
```

### Issues Fixed

- Fixes #19 (NameError during entity setup)
- Fixes #20 (Blocking I/O warnings with HA 2026.2.1)

### 🧪 Testing

- Tested on Home Assistant 2024.5.0+ and 2026.2.1
- All 254 unit tests passing
- Linting clean (ruff)
- No breaking changes to functionality

### 📦 Installation

Update through HACS or manually replace the `custom_components/adaptive_cover_pro` directory with the new version. Restart Home Assistant to apply the fix.

**Note:** This is a critical compatibility fix for Home Assistant 2026.2.1 (Python 3.13). Users experiencing `NameError` or blocking I/O warnings should update immediately.
