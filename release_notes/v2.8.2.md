## 🐛 Bug Fix Release

### 🐛 Bug Fixes

- Fix `AttributeError: 'ClimateCoverData' object has no attribute '_use_lux'` that crashed all cover entities after upgrading to v2.8.0 (#86)
- Fix blind spot diagnostic always reporting `null` instead of the actual blind spot status

### 🧪 Testing

- 752 tests passing
- Fixed test mocks that masked the above bugs by using unspec'd `MagicMock`

### 📦 Installation

Update via HACS or download the release ZIP and extract to `custom_components/adaptive_cover_pro`.
